package com.example.feature_home.home.presentation.state;

/**
 * One-off actions the Home UI should execute in response to user input.
 */
public enum HomeViewEvent {
    NAVIGATE_TO_MATCHING,
    SHOW_GAME_MODE_DIALOG,
    NAVIGATE_TO_SCORE,
    SHOW_RANKING_DIALOG,
    SHOW_SETTING_DIALOG
}
